name: Docker Image CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: 
          docker build -t vineet880/hello-fast-api .
          docker login -u vineet880 -p dckr_pat_DngVa2IHzGgBuzcvE94HL7j-yGs
          docker push vineet880/hello-fast-api:version1
